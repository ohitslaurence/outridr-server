# Implementation Plans

Generated by the improve skill on 2026-07-17 (audit at commit `a16bc22`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Selection note: the audit ran non-interactively, so plans were written for the
top findings by leverage (impact ÷ effort, weighted by confidence) per the
skill's default. The full findings table, including items *not* planned, is in
the audit summary delivered with this run; rejected/deferred items are listed
below so they aren't re-audited.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Test baseline (node:test harness), CI, LICENSE | P1 | M | — | DONE (executed 2026-07-17, reviewed & merged to main as `703bba1`) |
| 002  | herdrRequest double-callback fix + request-lifecycle hardening | P1 | S | 001 | DONE (executed 2026-07-17, reviewed & merged to main as `a2f014d`) |
| 003  | WebSocket hardening (head buffer, size caps, masking) | P1 | M | 001 | DONE (executed 2026-07-17, reviewed & merged to main as `e1a5dff`) |
| 004  | Tailscale bind recovery (retry, then fail fast) | P2 | S | 001, 002 | DONE (executed 2026-07-17, reviewed & merged to main as `1447280`) |
| 005  | Push token lifecycle (prune dead tokens, unregister, watcher cleanup) | P2 | M | 001, 002 | DONE (executed 2026-07-17, reviewed & merged to main as `8ee9519`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always.** Every other plan lands its fix with regression tests
  written into 001's harness (`test/*.test.mjs`, `node --test`, zero deps).
- 002 before 004: both edit `startServer`; 004 assumes 002's
  `server.on("error")` + exit-nonzero convention is already in place.
- 002 before 005: the push-watcher tests in 005 rely on `herdrRequest`
  firing its callback exactly once (002's settle guard).
- 003 is independent of 002/004/005 (disjoint sections of `lib/server.mjs`)
  but expect trivial line-number drift; each plan's drift check covers it.

## Findings considered and rejected / deferred

- **Split `lib/server.mjs` (663 lines, five concerns) into modules** — deferred:
  real but low-leverage now; the plan-001 integration tests are deliberately
  written against HTTP/WS so a future split is safe and mechanical.
- **Synchronous fs I/O in the `/session` request path** (`statSync`/`readSync`
  of ≤512 KiB windows) — not worth doing: single-user local-disk workload;
  blocking time is microseconds-to-low-ms. Revisit only if multi-client use
  appears.
- **No negative caching in `findSessionFile`** (unknown session id rescans all
  project dirs per request) — not worth doing at current scale; note kept here
  for the record.
- **`?token=` auth in query strings** — by design (WS clients that can't set
  headers); documented in README. Not a finding.
- **`/exec` passes client-supplied args to the configured binary** — by
  design and documented ("runs exactly the one configured binary with
  client-supplied args", opt-in, tailnet + token gated). The security posture
  depends on the operator's choice of binary; README already says so.
- **herdr socket has no auth of its own** — documented security model
  (tailnet ACLs + optional token); out of this repo's control.
- **`launchctl load/unload` deprecated in favor of `bootstrap/bootout`** —
  deferred: still functional on current macOS; revisit when Apple breaks it.
- **Plist/unit path escaping** (XML-significant chars in install paths) —
  deferred: pathological install paths only; would surface immediately at
  install time.
- **Expo receipts API polling and >100-message batching** — deferred; see
  plan 005 maintenance notes.
- **Tailscale IP change while running leaves a stale bind** — deferred; see
  plan 004 maintenance notes.
